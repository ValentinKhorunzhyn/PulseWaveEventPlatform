🛠️ Pulse Wave Event Platform - приложение для генерации и регистрации событий. Событийно - ориентированная архитетура.
Построено на:
- Java 21
- Spring Boot 4+
- Spring Framework 7
- Spring Data JPA + Hibernate
- Spring Security
- Lombok
- FlyWay
- Apache Kafka
- PostgreSQL
- Docker
- Maven

🧩 Обзор  
Бэкенд-система, позволяющая пользователям управлять данными событий с полной поддержкой CRUD-функций.
3 сервиса Publisher генерируют случайные события (USER_ACTION, SYSTEM_ALERT, BUSINESS_EVENT), сохраняют данные события в 
общую базу данных и затем отправляют информацию сервису регистратору Registar через брокера сообщений. Сервис Registar
принимает информацию от брокера и сохраняет событие в своей базе данных. Затем регистратор обрабатывает событие с учетом 
бизнес логики и при удачной обработке отправляем подтверждение генератору событий через брокера сообщений. Генератор
считывает сообщения с подтверждениями, по идентификатору находит в своей базе данных событие и помечает событие
как обработанное. 

📦 Возможности:  
✅ CRUD-операции (в основном чтение) для событий генератора и регистратора.  
✅ Получение статистики по событиям от генератора и регистратора.  
✅ Запуск генератора либо регистратора в Docker.  

🔐 Безопасность:
В приложении реализована базовая аутентификация по имени и паролю  
🔍 Функция поиска: реализована пагинация, сортировка и поиск событий по определенным фильтрам на стороне регистратора.

🧪 Дополнительно:  
✅ Документация API через Swagger.   
✅ Миграции базы данных (Flyway).  
✅ Формирование и кеширование статистики на стороне регистратора.   
✅ Реализована поддержка повторной отправки неудачно обработанных событий.  
✅ Реализована единая точка входа для получения статистики от генератора (поскольку у нас работает 3 сервиса ).

📦 Как запустить  
Через Docker:  
В корне проекта в терминале выполнить команду: docker-compose up -d --build  
Доступ к сервисам:  
http://localhost:8085 — Api Gateway. (для генераторов)  
http://localhost:8091 — Сервис генератор 1.  
http://localhost:8092 — Сервис генератор 2.  
http://localhost:8093 — Сервис генератор 3.  
http://localhost:8094 — Сервис регистратор.  
http://localhost:9092 — Брокер сообщений Kafka.  

📁Примечание:
- Топик в брокере для получения сообщений от генератора сразу состоит из 3 партиций поскольку генераторы отправляют 
событие по определенному ключу и брокер корректно распределяет сообщения.
- На стороне регистратора работают одновременно 3 потока чтения событий из брокера сообщений
- В рамках тестового задания сервисы и мапперы не имплементят интерфейсы, чтобы не нагромождать файлами структуру проекта
- В базах данных на таблицы не добавлены индексы поскольку у данных достаточно низкая селективность (кроме JSON данных)
- В регистраторе реализован глобальный обработчик ошибок
- Тестировать API лучше через Postman -> импортировать JSON сваггера как коллекцию (Swagger часто зависает) 

Планы:  
- Реализовать паттерн Transactional Outbox/Inbox поскольку сохранять сообщения в базу данных и отправлять их в брокера 
-> не является хорошей и устойчивой практикой
- Реализовать более качественную логику идемпотентности консьюмеров, а не просто игнорирование дубликатов
- Для кеширования статистических данных подключить Redis 
- Реализовать более сложную систему безопасности (Oauth2 либо подключить KeyCloak)
- Написать юнит и интеграционные тесты
- Вынести настройки для Docker профиля в отдельный файл
- Реализовать централизованную сборку логов и использовать сервисы мониторинга
